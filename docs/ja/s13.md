---
title: "Snap-inを自作する"
level: "advanced"
duration: 60
---

# Snap-inを自作する

> "npmパッケージのように公開・配布できる。ローカルテストから本番デプロイまでを一気通貫で学ぶ。"

## 問題

マーケットプレイスにない機能を自社で実装したい。たとえば「特定の条件でチケットが作成されたら社内の独自システムにデータを送信する」「毎朝9時に未対応チケットのサマリーをSlackに投稿する」など、自社固有の要件を満たすカスタム連携が必要になる。

## 解決策

DevRev CLIとTypeScriptを使ってSnap-inを開発する。Snap-inはnpmパッケージと同じようにバージョン管理・公開・配布できる構造になっている。

## 仕組み

**開発環境のセットアップ**

Snap-in開発には以下のツールが必要。

| ツール | 用途 |
|--------|------|
| Node.js | ランタイム |
| DevRev CLI (`devrev`) | プロジェクト作成、テスト、デプロイ |
| TypeScript | Snap-inのロジック記述 |

DevRev CLIのインストールは以下のコマンドで行う。

```bash
npm install -g @devrev/cli
```

**プロジェクト構造**

DevRev CLIでSnap-inプロジェクトを作成すると、以下の構造が生成される。

```
my-snap-in/
  ├── manifest.yaml        # Snap-inの定義ファイル
  ├── src/
  │   └── functions/
  │       └── handler.ts   # Function handler
  ├── package.json
  └── tsconfig.json
```

**manifest.yaml**

manifest.yamlはSnap-inの設計図である。以下の要素を定義する。

- **version**: Snap-inのバージョン
- **functions**: 実行するFunction群の定義
- **event_sources**: トリガーとなるイベント
- **keyrings**: 必要な外部認証情報

**Function handler**

Function handlerはSnap-inのビジネスロジックを記述する場所である。イベントを受け取り、処理を実行し、結果を返す。

```typescript
import { client } from "@devrev/typescript-sdk";

export async function handleEvent(event: any) {
  // イベントの内容に応じた処理を記述
  const devrevClient = client.setupBeta({
    endpoint: event.context.dev_oid,
    token: event.context.secrets.service_account_token,
  });

  // 例: チケットにコメントを追加
  await devrevClient.timelineEntriesCreate({
    object: event.payload.work_id,
    body: "Snap-inからの自動コメント",
    type: "timeline_comment",
  });
}
```

**ローカルテスト**

DevRev CLIにはローカルテスト機能がある。実際にデプロイする前に、手元の環境でFunctionの動作を確認できる。

```bash
devrev snap_in test
```

**デプロイフロー**

1. `devrev snap_in draft`: ドラフトとしてアップロード
2. DevRevのUI上でSnap-inを有効化
3. テスト環境で動作確認
4. 本番ワークスペースにインストール

**Snap-kit：UIコンポーネント**

Snap-inはバックグラウンド処理だけでなく、DevRevのUI内にカスタムインターフェースを埋め込むこともできる。この仕組みが**Snap-kit**である。

Snap-kitを使うと、Snap-inの設定画面やアクション実行時のモーダルを独自のコンポーネントで構築できる。たとえば、Snap-inのインストール時に認証情報を入力するフォームや、チケット詳細画面に表示するカスタムパネルなどが該当する。

| 用途 | 説明 |
|------|------|
| 設定フォーム | Snap-inインストール時の認証情報・設定値入力UI |
| アクションモーダル | コマンド実行時に表示する確認・入力ダイアログ |
| カスタムパネル | チケット・Issue詳細画面への情報埋め込み |

Snap-kitはmanifest.yamlの中で宣言し、ReactライクなコンポーネントモデルでUIを記述する。バックエンドのFunction handlerとセットで使うことで、「入力を受け取って処理を実行し、結果を表示する」一連のUIフローを完結させられる。

UIが必要ないシンプルな自動化（イベント検知→外部API呼び出し等）ではSnap-kitは不要である。チームや管理者が操作するインターフェースが必要な場合に採用を検討する。

## このセッションで学ぶこと

**始める前の状態：**
- Snap-in開発の経験がない
- manifest.yamlの構造を知らない
- ローカルテストの方法を知らない

**このセッション後の状態：**
- DevRev CLIでSnap-inプロジェクトを作成できる
- manifest.yamlの主要要素を説明できる
- Function handlerを記述し、ローカルテストとデプロイを実行できる

## 試してみる

Hello WorldのSnap-inをローカルで動かしてみよう。

```
【Snap-in開発演習】

□ 1. DevRev CLIをインストールした
□ 2. CLIでログインした
□ 3. 新規Snap-inプロジェクトを作成した
□ 4. manifest.yamlの内容を確認した
□ 5. Function handlerにログ出力を追加した
□ 6. ローカルテストを実行した
□ 7. ドラフトとしてアップロードした
□ 8. （発展）Snap-kitでUIコンポーネントを追加する場面を考えた

【作成したSnap-inの概要】
名前: ______________
トリガー: ______________
アクション: ______________
Snap-kit必要か: はい / いいえ（理由: ______________）
```

次のセッションでは、Agent Studioで自律エージェントを設計する。
